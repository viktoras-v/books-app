- hosts: all
  become: true
  gather_facts: no
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
  tasks:
    - name: Install packages
      apt:
        name:
          - docker.io
          - git
        update_cache: yes

    - name: Create cli-plugins directory
      file:
        path: /usr/local/lib/docker/cli-plugins
        state: directory
        mode: 0755

    - name: Enable Docker
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Add user to docker group
      user:
        name: ubuntu
        groups: docker
        append: yes

    - name: Clone repo
      git:
        repo: https://github.com/viktoras-v/books-app.git
        dest: /opt/books-app
        force: yes

        
    - name: Run application
      shell: docker compose up -d --build
      args:
        chdir: /opt/books-app